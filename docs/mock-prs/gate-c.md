# Mock Gate C

This fixture proposes a repository policy change that needs a maintainer
decision before implementation.

Open questions:

- Should contribution routing rules be fixed defaults or repository-specific
  configuration?
- Which GitHub writes are allowed from local CLI, dashboard, Action, and GitHub
  App contexts?
- Which write paths need explicit approval before they run?

The fixture is documentation-only, but it intentionally leaves product and
permission decisions unresolved.
